spring:
  profiles:
    active: "test"
  application:
    name: "associado-tests"
  main:
    banner-mode: off

logging:
  file:
    name: "${logging.file.path}/${START_TIME}.log"
    path: "tmp/logs/${spring.application.name}/tests"
  level:
    org.springframework: off
